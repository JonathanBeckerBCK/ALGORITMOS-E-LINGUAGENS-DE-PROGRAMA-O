programa
{
	
	funcao inicio()
	{
	   escreva("3. Escreva um algoritmo que leia 4 notas, mostre as notas e a média na tela. As notas devem ser armazenadas em um vetor de 4 posições. A leitura das notas deve ser realizada dentro de um laço de repetição.\n")
					   // 0,1,2,3,4
	     cadeia vetor[4]={"a","b","c","d"}	

		   leia(vetor[0])
		   escreva(vetor[0])

		   leia(vetor[1])
		   escreva(vetor[0])

		   leia(vetor[0])
		   escreva(vetor[0])

		   leia(vetor[0])
		   escreva(vetor[0])
	
	}
}
